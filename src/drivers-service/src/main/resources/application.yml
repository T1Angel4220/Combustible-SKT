server:
  port: 8081
  
spring:
  application:
    name: drivers-service
  
  datasource:
    url: jdbc:postgresql://localhost:5432/drivers_db
    username: ${DB_USERNAME:drivers_user}
    password: ${DB_PASSWORD:drivers_pass}
    driver-class-name: org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        
  profiles:
    active: dev

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Logging configurations  
logging:
  level:
    com.skt.combustible.drivers: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# gRPC Configuration
grpc:
  server:
    port: 9091
    
# Service Discovery (para comunicación entre microservicios)
service:
  urls:
    vehicles-service: http://localhost:8082
    routes-service: http://localhost:8083
    fuel-service: http://localhost:8084
    auth-service: http://localhost:8085
