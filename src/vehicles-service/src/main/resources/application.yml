server:
  port: 8082
  
spring:
  application:
    name: vehicles-service
  
  datasource:
    url: jdbc:postgresql://localhost:5432/vehicles_db
    username: ${DB_USERNAME:vehicles_user}
    password: ${DB_PASSWORD:vehicles_pass}
    driver-class-name: org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        
  profiles:
    active: dev

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Logging configurations  
logging:
  level:
    com.skt.combustible.vehicles: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# gRPC Configuration
grpc:
  server:
    port: 9092
    
# Service Discovery (para comunicación entre microservicios)
service:
  urls:
    drivers-service: http://localhost:8081
    routes-service: http://localhost:8083
    fuel-service: http://localhost:8084
    auth-service: http://localhost:8085
