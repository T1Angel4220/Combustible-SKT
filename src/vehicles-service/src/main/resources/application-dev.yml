spring:
  data:
    mongodb:
      uri: mongodb://vehicles_user:vehicles_pass@localhost:27017/vehicles_db?authSource=admin

logging:
  level:
    org.springframework.data.mongodb: DEBUG
    org.mongodb.driver: DEBUG

